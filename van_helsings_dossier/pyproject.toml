[project]
name = "van-helsings-dossier"
version = "0.1.0"
description = "Compile transcript sources into Obsidian-friendly daily Markdown journal files"
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0.0",
]
