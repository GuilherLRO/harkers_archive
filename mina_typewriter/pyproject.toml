[project]
name = "mina-typewriter"
version = "0.1.0"
description = "Batch-transcribe audio and video with the OpenAI Audio API (CLI + Streamlit)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "streamlit>=1.40.0",
]
