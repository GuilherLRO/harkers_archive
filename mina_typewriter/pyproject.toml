[project]
name = "mina-typewriter"
version = "0.1.0"
description = "Batch-transcribe audio and video with OpenAI Whisper (CLI + Streamlit)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai-whisper>=20240930",
    "python-dotenv>=1.0.0",
    "streamlit>=1.40.0",
]
