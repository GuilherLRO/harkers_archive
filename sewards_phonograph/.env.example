# Copy to .env in this directory, or export these variables in your shell.
TELEGRAM_BOT_TOKEN=your-token-from-botfather
# Absolute path to the shared voice archive at the monorepo root:
SAVE_DIR=/absolute/path/to/harkers_archive/voice_archive
