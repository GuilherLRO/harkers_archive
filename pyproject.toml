[project]
name = "harkers-archive"
version = "0.1.0"
description = "Personal record base from voice and text notes: capture, transcribe, and future modules"
readme = "README.md"
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0.0",
    "python-telegram-bot>=21.0",
]

[tool.uv.workspace]
members = ["sewards_phonograph", "mina_typewriter"]
